---
phase: 05-revision-and-publication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/posts/software-quality-age-of-ai.mdx
autonomous: true
requirements:
  - QUAL-01
  - QUAL-04
  - TECH-01
  - TECH-02
  - TECH-03

must_haves:
  truths:
    - "Post word count is 1700-1800 words as measured by wc -w on the full MDX file"
    - "Reader walks away feeling both reassured (their role matters) AND challenged (they must claim it explicitly)"
    - "No manifesto register violations in expanded prose: no hedging, no passive voice on claims, no em dashes, no tool names, no adapt-or-die rhetoric"
    - "Locked closing line 'This is the work now, and it's yours.' remains the absolute final prose sentence"
    - "QUAL-03 balance preserved: blind spot + pivot section still equal or greater word count than force multiplier section"
    - "npm run build passes with post at /blog/software-quality-age-of-ai"
    - "Post appears on blog listing page as most recent post (date 2026-03-01)"
  artifacts:
    - path: "src/content/posts/software-quality-age-of-ai.mdx"
      provides: "Final published blog post"
      contains: "This is the work now, and it's yours."
  key_links:
    - from: "src/content/posts/software-quality-age-of-ai.mdx"
      to: "src/lib/posts.ts"
      via: "getAllPosts() filesystem read and date sort"
      pattern: "export const metadata"
---

<objective>
Expand the draft by ~260-360 words (targeting 1,700-1,800 total via wc -w), concentrated in the synthesis and close sections, while preserving manifesto register and argument balance. Then verify build, word count, and listing sort order.

Purpose: The draft is 1,439 words (wc -w), below the 1,500 minimum and well below the 1,700-1,800 target. TECH requirements are already met. This plan closes the word count gap and verifies publication readiness.

Output: Final blog post at target word count, build verified, publication confirmed.
</objective>

<execution_context>
@/Users/kraigbritton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kraigbritton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-revision-and-publication/05-RESEARCH.md
@.planning/phases/04-draft-hook-reframe-and-close/04-01-SUMMARY.md
@docs/plans/2026-03-01-software-quality-age-of-ai-design.md

<interfaces>
<!-- The MDX post follows this exact pattern — no changes to structure needed -->

From src/content/posts/software-quality-age-of-ai.mdx:
```typescript
export const metadata = {
  title: "Software Quality in the Age of AI",
  date: "2026-03-01",
  description: "AI makes quality engineers more powerful than ever, but only if they claim the one role AI cannot fill: deciding what quality means.",
};
```

From src/lib/posts.ts:
```typescript
// Posts are read from filesystem via dynamic import, sorted by date descending
// slug derived from filename, metadata destructured from mod.metadata
// Three required fields: title (string), date (YYYY-MM-DD string), description (string)
```
</interfaces>

<revision_constraints>
These constraints are non-negotiable. They are distilled from the design doc and four prior phases of decisions:

EXPAND ONLY these sections:
1. Synthesis ("Quality Systems Thinking Is the Job Now") — currently ~250 words, target ~350-400. The three activities (design gates, own risk, shift left/right) each have one paragraph. Add a second concrete sentence or short paragraph per activity that deepens the argument with a specific consequence or implication. Target ~100-150 additional words here.
2. Close (the paragraphs after the synthesis activities, starting "The fear was real...") — currently ~170 words, target ~270-320. The challenge beats before the vacuum image have room for one or two additional concrete consequence sentences about what happens when quality thinking is NOT claimed. Target ~100-150 additional words here.

DO NOT EXPAND these sections:
- Hook (opening through "only one of them is yours to solve") — 182 words, carefully calibrated in Phase 4
- Force multiplier example ("## AI Gave One Engineer...") — 347 words at design target; expansion risks QUAL-03 balance
- Blind spot example ("## The Test Suite Said Green...") — 456 words with pivot at design target; expansion risks QUAL-03 balance
- Pivot sentence (paragraph starting "The same force that let one engineer...") — tight by design

REGISTER RULES for all new prose:
- No hedging: no "might," "could," "perhaps" in thesis claims
- Active voice on all claims
- No em dashes anywhere
- No tool or vendor names
- No "adapt or die" urgency rhetoric
- Italics allowed selectively for emphasis
- Each new sentence must make a direct claim or state a concrete consequence

LOCKED LINE: "This is the work now, and it's yours." must remain the absolute final prose sentence. Nothing after it.

QUAL-04 BALANCE: After expansion, the post must not tip into pure comfort (job is safe without action) or pure alarm (outcome is inevitable regardless of action). The balance is achieved when the reader feels challenged to act.
</revision_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand synthesis and close sections to reach 1,700-1,800 word target</name>
  <files>src/content/posts/software-quality-age-of-ai.mdx</files>
  <action>
Read the full MDX file. Then expand prose in two specific locations, following the revision constraints above exactly.

**Expansion Site 1: Synthesis section (~100-150 words added)**

The section under "## Quality Systems Thinking Is the Job Now" currently has three activity paragraphs (design gates, own risk, shift left/right). For each activity, add one or two concrete sentences that deepen the argument. Approaches:

- "Design quality gates" paragraph: Add a sentence about what happens without gates. The absence of a gate is not neutral. Without a human decision about what "done" means, AI generates to its own definition of completeness.
- "Own risk assessment" paragraph: Add a concrete sentence about the *kind* of judgment involved. Severity triage, business impact ranking, failure mode analysis. These are quality discipline skills that predate AI and become more critical with AI velocity.
- "Shift left AND right" paragraph: Add a sentence about how AI extends the surface area in both directions. More code generated means more acceptance criteria needed upstream. More systems deployed means more production quality signals needed downstream.

**Expansion Site 2: Close section (~100-150 words added)**

The close begins at "The fear was real. The mechanism was wrong." and ends with the locked line. Currently 170 words. Add concrete consequence sentences between the vacuum image paragraph and the locked line, or between the "claim it explicitly" paragraph and the vacuum paragraph. Possible additions:

- A sentence about the professional identity shift: quality systems thinking is not a niche specialization. It is the core competency of every quality role now.
- A sentence about what "claiming" looks like in practice: it is not waiting for a mandate. It is writing the acceptance criteria before anyone asks. It is naming the risk before the ticket is closed.
- A sentence about the timeline: the window to claim this work is not abstract. Teams are forming habits around AI right now. The quality patterns they establish in the next year will calcify.

**After expanding, verify:**
1. Run `wc -w src/content/posts/software-quality-age-of-ai.mdx` — must be 1700-1800
2. Verify locked line "This is the work now, and it's yours." is still the final prose sentence
3. Verify no em dashes in the file
4. Verify no hedging words ("might," "could," "perhaps") in any thesis claims or section arguments
5. Verify QUAL-03: count words in blind spot section (from "## The Test Suite Said Green" through the pivot paragraph ending "share a source") and force multiplier section (from "## AI Gave One Engineer" through "everything your quality function can do"). Blind spot + pivot must be >= force multiplier.
6. Read the full post for QUAL-04 balance: does it tip into pure comfort or pure alarm? If yes, adjust.
  </action>
  <verify>
    <automated>cd /Users/kraigbritton/working/personal-website && WC=$(wc -w < src/content/posts/software-quality-age-of-ai.mdx) && echo "Word count: $WC" && test "$WC" -ge 1700 && test "$WC" -le 1850 && echo "PASS: word count in range" && tail -1 src/content/posts/software-quality-age-of-ai.mdx | grep -q "This is the work now, and it's yours." && echo "PASS: locked line is final" && ! grep -q "—" src/content/posts/software-quality-age-of-ai.mdx && echo "PASS: no em dashes"</automated>
  </verify>
  <done>Post is 1700-1800 words (wc -w), synthesis and close expanded with register-compliant prose, locked line remains final sentence, no register violations, QUAL-03 balance preserved, QUAL-04 balance maintained</done>
</task>

<task type="auto">
  <name>Task 2: Verify build and publication readiness</name>
  <files>src/content/posts/software-quality-age-of-ai.mdx</files>
  <action>
Run `npm run build` from the project root. Verify:

1. Build completes without errors
2. The route `/blog/software-quality-age-of-ai` appears in the build output
3. Static pages are generated successfully

This confirms TECH-01 (MDX file with correct metadata), TECH-02 (build passes), and TECH-03 (post appears on listing sorted by date).

If build fails, check for MDX syntax issues introduced during expansion (especially `<`, `>`, `{`, `}` characters in prose that could break JSX parsing). Fix any issues and rebuild.
  </action>
  <verify>
    <automated>cd /Users/kraigbritton/working/personal-website && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>npm run build passes, /blog/software-quality-age-of-ai route present in output, all static pages generated</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full set of phase requirements:

1. **QUAL-01:** `wc -w src/content/posts/software-quality-age-of-ai.mdx` returns 1700-1800
2. **QUAL-04:** Full post read confirms balance between reassurance and challenge
3. **TECH-01:** MDX file exists at `src/content/posts/software-quality-age-of-ai.mdx` with metadata export containing title, date, description
4. **TECH-02:** `npm run build` exits 0
5. **TECH-03:** Post appears at `/blog/software-quality-age-of-ai` in build output, sorted as most recent post (date 2026-03-01)
</verification>

<success_criteria>
- Word count: 1700-1800 (wc -w on full file)
- Build: passes without errors
- Locked line: "This is the work now, and it's yours." is the final prose sentence
- Register: no em dashes, no hedging in claims, active voice throughout
- Balance: blind spot + pivot >= force multiplier word count (QUAL-03 preserved)
- Tone: reader feels both reassured AND challenged (QUAL-04)
- Publication: post visible at /blog/software-quality-age-of-ai, sorted as most recent
</success_criteria>

<output>
After completion, create `.planning/phases/05-revision-and-publication/05-01-SUMMARY.md`
</output>
