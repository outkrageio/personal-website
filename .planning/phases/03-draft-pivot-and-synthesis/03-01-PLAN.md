---
phase: 03-draft-pivot-and-synthesis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/posts/software-quality-age-of-ai.mdx
autonomous: true
requirements: [ARG-03, ARG-05, QUAL-02, QUAL-03]

must_haves:
  truths:
    - "Quality systems thinking is named AFTER both examples, not before"
    - "Three specific activities are present: designing quality gates, owning risk assessment, shifting left AND right"
    - "Each activity references at least one specific example detail (discount eligibility, diagnostic service)"
    - "No hedging language, no urgency rhetoric, no tool names in the synthesis"
    - "Blind spot example word count >= force multiplier word count"
    - "Register is warm conviction, not crescendo. Energy peak is reserved for Phase 4 close"
  artifacts:
    - path: "src/content/posts/software-quality-age-of-ai.mdx"
      provides: "Synthesis section (200-250 words) inserted after pivot, before fear narrative placeholder"
      contains: "quality systems thinking"
  key_links:
    - from: "synthesis section"
      to: "force multiplier example"
      via: "activity paragraphs referencing diagnostic service details"
      pattern: "diagnostic|ranked|confidence"
    - from: "synthesis section"
      to: "blind spot example"
      via: "activity paragraphs referencing discount eligibility and lapse condition"
      pattern: "discount|eligibility|lapse|consecutive"
    - from: "pivot paragraph"
      to: "synthesis section"
      via: "structural position in MDX (pivot ends, synthesis heading follows)"
      pattern: "velocity without human quality gates"
---

<objective>
Write the argument's resolution: the quality systems thinking synthesis section (200-250 words) that names and defines the concept through three specific activities, inserted after the existing pivot paragraph and before the fear narrative placeholder.

Purpose: This section is the intellectual resolution of the post's two examples. The force multiplier showed what AI enables; the blind spot showed what AI misses. The synthesis names the gap and defines the work that fills it. Without this section, the post has tension without resolution.

Output: Updated `src/content/posts/software-quality-age-of-ai.mdx` with synthesis section inserted at the correct structural position.
</objective>

<execution_context>
@/Users/kraigbritton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kraigbritton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-draft-pivot-and-synthesis/03-CONTEXT.md
@.planning/phases/03-draft-pivot-and-synthesis/03-RESEARCH.md
@docs/plans/2026-03-01-software-quality-age-of-ai-design.md
@src/content/posts/software-quality-age-of-ai.mdx

Prior plan context (needed for word counts and content structure):
@.planning/phases/02-draft-examples/02-02-SUMMARY.md

<interfaces>
<!-- Current MDX file structure (Phase 2 output). Executor writes into this file. -->

Current structure:
```
metadata export (lines 1-5)
## AI Gave One Engineer the Power of a Team        (lines 7-24, ~348 words)
## The Test Suite Said Green...                    (lines 26-43, ~398 words + pivot)
[pivot paragraph at line 43]                       (~61 words, ends with "The opportunity and the risk share a source.")
{/* --- FEAR NARRATIVE PLACEHOLDER --- */}         (line 45)
[fear narrative content]                           (lines 47-55, ~134 words)
{/* --- END FEAR NARRATIVE PLACEHOLDER --- */}     (line 55, last line is 56)
```

Insertion point: AFTER line 43 (pivot paragraph ending "The opportunity and the risk share a source."), BEFORE line 45 (`{/* --- FEAR NARRATIVE PLACEHOLDER --- */}`).

Key locked phrases in pivot (do NOT rewrite):
- "Velocity without human quality gates is the mechanism."
- "The opportunity and the risk share a source."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write synthesis section and insert into MDX</name>
  <files>src/content/posts/software-quality-age-of-ai.mdx</files>
  <action>
Write a 200-250 word synthesis section and insert it into the MDX file after the pivot paragraph (line ending "The opportunity and the risk share a source.") and before the fear narrative placeholder comment (`{/* --- FEAR NARRATIVE PLACEHOLDER --- */}`).

The synthesis section follows the earned declaration structure (three moves):

**Move 1: Bridge from examples + name the concept (40-50 words)**
- Open with first-person authority ("I" voice) arriving from the examples. The first sentence of the section must connect to what the reader just experienced, not announce a framework.
- Name "quality systems thinking" in sentence two or three. The name lands as recognition, not instruction. The reader should already feel what it means before seeing the label.
- Do NOT open with "Quality systems thinking is..." as the first sentence. The name must arrive, not announce itself.

**Move 2: Declarative listing sentence**
- One flat sentence naming all three activities: designing quality gates, owning risk assessment, shifting left AND right.
- Voice note from design doc: "Quality systems thinking is three things: designing gates, owning risk, shifting left and right." This declarative structure should survive.

**Move 3: One paragraph per activity wired to examples (150-180 words total)**
- Shift to second-person ("you/your") for the activity paragraphs. Author earned authority from first-person experience; now redirects it at the reader.
- Each activity paragraph references at least one specific example detail (not generic advice):

  Activity 1 — Designing quality gates: Wire to blind spot example. Reference the acceptance criteria tests that caught the discount eligibility failure. The gate was a human decision about what "twelve consecutive months" meant. Wire to force multiplier if budget allows: gates enabled by diagnostic service's confidence-ranked output.

  Activity 2 — Owning risk assessment: Wire to blind spot example. The business stakes of a wrong discount rule (real money, real customers) are human judgments AI could not assign. The cost was not a test suite metric.

  Activity 3 — Shift left AND right: Wire to both examples. Force multiplier shifts quality right (diagnostic service extends quality into production telemetry). Blind spot shifts quality left (acceptance criteria written before code generation shifts quality into requirements). Use the exact phrase "shift left AND right" with AND emphasized or capitalized. The AND is the insider twist.

**Register constraints (enforced):**
- Warm conviction: measured authority with human undertone. Not cold, not heated.
- No crescendo: energy peak is saved for Phase 4 close. The synthesis ends with confidence, not heat. The final beat uses "yours" for possession and professional responsibility.
- No backward reference to the fear narrative. The synthesis looks forward. Phase 4 handles the explicit reframe.
- No hedging: "you design" not "you might consider." Direct on all claims and activities.
- No tool names, vendor names, or framework references.
- No em dashes. Use periods or commas.
- No "adapt or die" urgency rhetoric.
- No "might" / "could" / "perhaps" on thesis claims or activity statements.

**Pivot sentence refinement:** If the transition from the existing pivot paragraph to the new synthesis heading feels abrupt, add a single transitional sentence at the START of the synthesis section rather than editing the pivot paragraph. The pivot paragraph contains locked phrases ("velocity without human quality gates," "The opportunity and the risk share a source") that must survive unchanged.

**Heading:** Use a ## heading for the synthesis section. Claude's discretion on exact text, but it must be a claim (argument), not a label. Design doc provisional: "Quality Systems Thinking Is the Job Now" is strong. Test any alternative against this benchmark.
  </action>
  <verify>
    <automated>cd /Users/kraigbritton/working/personal-website && node -e "
const fs = require('fs');
const content = fs.readFileSync('src/content/posts/software-quality-age-of-ai.mdx', 'utf8');
const lines = content.split('\n');
let errors = [];

// Check synthesis section exists
if (!content.includes('quality systems thinking') && !content.includes('Quality Systems Thinking') && !content.includes('quality systems')) {
  errors.push('FAIL: quality systems thinking not found in content');
}

// Check three activities present
if (!content.toLowerCase().includes('quality gate')) errors.push('FAIL: quality gates activity not found');
if (!content.toLowerCase().includes('risk assessment') && !content.toLowerCase().includes('risk')) errors.push('FAIL: risk assessment activity not found');
if (!content.toLowerCase().includes('shift left')) errors.push('FAIL: shift left AND right activity not found');

// Check no forbidden patterns
const forbidden = ['adapt or die', 'might consider', 'could involve', 'perhaps'];
forbidden.forEach(f => { if (content.toLowerCase().includes(f)) errors.push('FAIL: forbidden phrase found: ' + f); });

// Check structural order: pivot before synthesis heading before fear placeholder
const pivotIdx = content.indexOf('The opportunity and the risk share a source.');
const fearIdx = content.indexOf('FEAR NARRATIVE PLACEHOLDER');
const synthIdx = content.indexOf('## ');
// Find the synthesis heading (should be after second example heading)
const headings = [...content.matchAll(/^## .+$/gm)];
if (headings.length < 3) errors.push('FAIL: expected at least 3 ## headings (2 examples + synthesis)');
if (headings.length >= 3) {
  const synthHeadingStart = headings[2].index;
  if (synthHeadingStart < pivotIdx) errors.push('FAIL: synthesis heading appears before pivot paragraph');
  if (synthHeadingStart > fearIdx) errors.push('FAIL: synthesis heading appears after fear narrative placeholder');
}

// Check locked pivot phrases survived
if (!content.includes('Velocity without human quality gates is the mechanism')) errors.push('FAIL: locked pivot phrase missing');
if (!content.includes('The opportunity and the risk share a source')) errors.push('FAIL: locked pivot phrase missing');

if (errors.length === 0) console.log('PASS: All structural checks passed');
else errors.forEach(e => console.log(e));
"</automated>
  </verify>
  <done>
Synthesis section (200-250 words) inserted into MDX file between pivot paragraph and fear narrative placeholder. Quality systems thinking named after examples. Three activities present with explicit wiring to example details. Voice transitions from first-person bridge to second-person activities. No register violations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify word counts, register, and QUAL-03 balance</name>
  <files>src/content/posts/software-quality-age-of-ai.mdx</files>
  <action>
Run verification checks on the completed MDX file. Do NOT edit the file in this task unless a critical register violation is found.

**Word count checks:**
1. Count synthesis section words. Must be 200-250.
2. Count force multiplier section words (## AI Gave One Engineer... through end of that section).
3. Count blind spot section words (## The Test Suite Said Green... through end of pivot paragraph, before synthesis heading).
4. Confirm QUAL-03: blind spot word count >= force multiplier word count.

**Register checks (read the synthesis section and verify):**
- No hedging on claims or activities ("might," "could," "perhaps" on thesis/activity statements)
- No tool names, vendor names, AI provider names
- No em dashes (the -- character or the unicode em dash)
- No "adapt or die" urgency rhetoric
- No backward reference to fear narrative ("despite the fear," "the fear that," etc.)
- Voice transition present: first-person in bridge, second-person in activities
- Each activity paragraph contains at least one specific example reference (not generic)
- Register is measured, not crescendo. Section does not end with exclamation, imperative challenge, or direct call to action (those belong to Phase 4)

**Build check:**
Run `npm run build` to confirm the MDX compiles and the static export succeeds.

If any check fails, fix the specific issue in the synthesis section (minimal edit, not a rewrite). If QUAL-03 fails due to synthesis additions somehow shifting the balance, note for Phase 5 but do not edit existing example sections.
  </action>
  <verify>
    <automated>cd /Users/kraigbritton/working/personal-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
Word counts verified: synthesis 200-250 words, QUAL-03 satisfied (blind spot >= force multiplier). No register violations. Build succeeds.
  </done>
</task>

</tasks>

<verification>
1. `src/content/posts/software-quality-age-of-ai.mdx` contains synthesis section between pivot and fear narrative placeholder
2. Synthesis section is 200-250 words
3. Three activities named and wired to specific examples
4. QUAL-03: blind spot word count >= force multiplier word count
5. No forbidden language (hedging, tool names, em dashes, urgency rhetoric)
6. `npm run build` succeeds
</verification>

<success_criteria>
- The synthesis section reads as the post's intellectual resolution: the reader recognizes quality systems thinking as the name for what the two examples demonstrated
- Three activities are concrete and grounded in the post's own examples, not generic QA advice
- Word count is within 200-250 target
- QUAL-03 balance maintained
- Manifesto register intact: assertive, direct, warm conviction
- Build passes and post renders correctly
</success_criteria>

<output>
After completion, create `.planning/phases/03-draft-pivot-and-synthesis/03-01-SUMMARY.md`
</output>
